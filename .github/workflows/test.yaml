name: 'build-test'
on:
  # pull_request:
  push:
    branches:
      # - develop
      - main
      # - 'releases/*'

jobs:
  # prepare:
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - run: |
  #         npm install
  #     - run: |
  #         npm run build && npm run format && npm run lint && npm run package
  # - run: |
  #     npm run all
  build: # make sure the action works on a clean machine without building
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: "us-east-1"
    steps:
      - uses: actions/checkout@v2

      # - name: Cocos Creator Build Use ConfigPath
      #   uses: lyzz0612/cocos-build@1.0.1
      #   with:
      #     config_path: ./web-desktop.json

      - name: aws-s3-cloudfront
        uses: Comori/aws-s3@v0.1.3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
          bucket: "cocos-test.helix.city"
          # source-files: build/web-mobile
          source-files: |
            settings
          target-dir: "settings"
          
    
      - name: aws-s3-cloudfront
        uses: Comori/aws-s3@v0.1.3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
          invalidation-path: |
            /*
          distribution-id: "E1E042I8V90ZQT"
